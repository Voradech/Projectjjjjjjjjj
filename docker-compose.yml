
version: "3.9"

services:
  backend:
    build: ./backend
    container_name: predict-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: predict-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
 
